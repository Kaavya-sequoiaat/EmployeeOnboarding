<UserControl x:Class="EmployeeOnboarding.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             Background="#F4F6FA">

    <ScrollViewer>
        <Grid Margin="30">

            <UniformGrid Columns="3" Margin="0,0,0,30">
                <Border Padding="20">
                    <TextBlock Text="{Binding TotalEmployees}"
                               FontSize="32"
                               FontWeight="Bold"/>
                </Border>

                <Border Padding="20">
                    <TextBlock Text="{Binding AddedToday}"
                               FontSize="32"
                               FontWeight="Bold"/>
                </Border>

                <Border Padding="20">
                    <TextBlock Text="{Binding AddedThisMonth}"
                               FontSize="32"
                               FontWeight="Bold"/>
                </Border>
            </UniformGrid>

            <DataGrid ItemsSource="{Binding RecentEmployees}"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding FirstName}"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}"/>
                    <DataGridTextColumn Header="Added On" Binding="{Binding CreatedDate}"/>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </ScrollViewer>
</UserControl>
